import { motion } from "framer-motion";
import { SetSelectedPageType } from "@/utils/types";
import Class from "./Class";
import { LINKS_TYPE } from "@/utils/enums";
import TitleText from "@/components/TitleText";
import { CLASSES, FADERIGHT } from "@/utils/constants";

const OurClasses = ({ setSelectedPage }: SetSelectedPageType) => {
  return (
    <section id="ourclasses" className="w-full bg-primary-100 py-40">
      <motion.div
        onViewportEnter={() => setSelectedPage(LINKS_TYPE.OurClasses)}
      >
        <motion.div
          className="mx-auto w-5/6"
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, amount: 0.5 }}
          transition={{ duration: 0.5 }}
          variants={FADERIGHT}
        >
          <div className="md:w-3/5">
            <TitleText>OUR CLASSES</TitleText>
            <p className="py-5">
              Fringilla a sed at suspendisse ut enim volutpat. Rhoncus vel est
              tellus quam porttitor. Mauris velit euismod elementum arcu neque
              facilisi. Amet semper tortor facilisis metus nibh. Rhoncus sit
              enim mattis odio in risus nunc.
            </p>
          </div>
        </motion.div>
        <div className="mt-10 h-[353px] w-full overflow-x-auto overflow-y-hidden">
          <ul className="w-[2800px] whitespace-nowrap">
            {CLASSES.map((item, index) => (
              <Class
                key={`${item.name}-${index}`}
                name={item.name}
                description={item.description}
                image={item.image}
              />
            ))}
          </ul>
        </div>
      </motion.div>
    </section>
  );
};

export default OurClasses;
